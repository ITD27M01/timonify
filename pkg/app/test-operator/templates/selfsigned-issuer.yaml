apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "test-operator.fullname" . }}-selfsigned-issuer
  labels:
  {{- include "test-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}